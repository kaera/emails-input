import styles from './emails-input.css';

const defaultStyles = {
    root: 'root',
    tag: 'tag',
    valid: 'valid',
    invalid: 'invalid',
    remove: 'remove',
    input: 'input',
};

export default Object.assign(defaultStyles, styles);
